# React Native + pnpm requires hoisted node_modules so that Metro bundler
# and CocoaPods (iOS) can resolve packages via standard node_modules/<pkg> paths.
# Without this, pnpm's virtual store breaks react-native-worklets resolution
# during pod install, and Metro can't find native modules at runtime.
shamefully-hoist=true
